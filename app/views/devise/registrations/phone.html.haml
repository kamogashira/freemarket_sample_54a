.register-header
 .register-header_content
  %h1
   %a
    = image_tag "top_page/header-mercari.svg", size: '180x50', alt:"mercari"

  .header-bar
   %ol
    %li.info
     会員情報
     .border-radius_red
     .header-border_red

    %li.info 
     電話番号認証
     .border-radius_red
     .header-border

    %li 
     お届け先住所入力
     .border-radius
     .header-border

    %li 
     支払い方法
     .border-radius
     .header-border

    %li 
     完了
     .border-radius

.register 
 .register_container
  .register_container_view
   %h2.register-container_title 電話番号の確認
 
  = form_for @user, url: users_sign_up_address_path, method: :get, class: "container_form" do |f|
   .form-group
    .form-group_section
     = f.fields_for :user_address do |u|
      = u.label :phone_number, "携帯電話の番号"
      = u.text_field :phone_number, class: "usernew", placeholder: "携帯電話の番号を入力"
      %p.phone 本人確認のため、携帯電話のSMS(ショートメッセージサービス)を利用して認証を行います。

    .form-group_section
     = f.submit 'SMSを送信する', class:"next_btn"
     %p ※電話番号は本人確認や不正利用防止のために利用します。他のユーザーに公開されることはありません。
    .form-group_section
     = link_to "電話番号の確認が必要な理由", "https://www.mercari.com/jp/help_center/article/244"